from typing import Dict, Any

def compute(payload: Dict[str, Any]) -> Dict[str, float]:
    """
    TODO: replace with the real IPA calculator logic.
    For now returns stable placeholders so tests run.
    """
    return {
        "example_metric": 123.45,
        "another": 67.89,
    }
